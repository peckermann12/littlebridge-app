# =============================================================================
# LittleBridge Environment Variables
# =============================================================================
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# NEVER commit .env.local or any file containing real secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# Client-side (VITE_) — these are embedded in the built JS bundle
# -----------------------------------------------------------------------------

# Supabase — find at: https://supabase.com/dashboard/project/<your-project>/settings/api
VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key-here

# Stripe — find at: https://dashboard.stripe.com/apikeys
# Use test-mode keys (pk_test_) for development, live keys (pk_live_) for production.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Price ID for the $99/mo center subscription.
# Find at: https://dashboard.stripe.com/prices
VITE_STRIPE_PRICE_ID=price_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Base URL of the deployed app (no trailing slash)
VITE_APP_URL=http://localhost:5173
# Production: VITE_APP_URL=https://littlebridge.ai

# Verified sending address (must match your Resend domain)
VITE_FROM_EMAIL=hello@littlebridge.ai

# Default language for the UI (en or zh)
VITE_DEFAULT_LANGUAGE=en

# -----------------------------------------------------------------------------
# Supabase Edge Function secrets — server-side only, NEVER expose to browser.
# Set these via `supabase secrets set KEY=value` (not in .env.local).
# Listed here for reference so you know what needs to be configured.
# -----------------------------------------------------------------------------

# Supabase service role key — bypasses RLS. Handle with extreme care.
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...your-service-role-key-here

# Stripe secret key — find at: https://dashboard.stripe.com/apikeys
# Use test-mode keys (sk_test_) for development, live keys (sk_live_) for production.
# STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe webhook signing secret — from Stripe Dashboard > Webhooks > Signing secret
# For local dev, use the secret printed by `stripe listen`.
# STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Price ID (also needed server-side for checkout session creation)
# STRIPE_PRICE_ID=price_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Claude API (Anthropic) — find at: https://console.anthropic.com/settings/keys
# Used by the translate-text and compute-match Edge Functions.
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Resend (transactional email) — find at: https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Optional — SMS notifications via Twilio
# -----------------------------------------------------------------------------
# Used to send SMS alerts to centers when they receive a new enquiry.
# Not required for MVP — enquiry notifications are sent via email by default.
# Sign up at: https://www.twilio.com/try-twilio
# Find credentials at: https://console.twilio.com/
# TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TWILIO_PHONE_NUMBER=+1xxxxxxxxxx

# -----------------------------------------------------------------------------
# Optional — Error Tracking (Sentry)
# -----------------------------------------------------------------------------
# Find at: https://sentry.io/settings/<org>/projects/<project>/keys/
# VITE_SENTRY_DSN=https://xxxxxxxxxxxxxxxxxxxx@o0.ingest.sentry.io/0

# -----------------------------------------------------------------------------
# Optional — Analytics (PostHog)
# -----------------------------------------------------------------------------
# Find at: https://app.posthog.com/project/settings
# VITE_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# VITE_POSTHOG_HOST=https://app.posthog.com
